name: test
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print step
        run: |
          echo "Hello World"
          uptime
          whoami
          cat /etc/OS-release
